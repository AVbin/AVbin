AVbin

AVbin incorporates FFmpeg, http://ffmpeg.mplayerhq.hu/

To install, copy libavbin.*.dylib to a path such as /usr/local/lib and create
a symlink without the version number.  Or, run the included ``install.sh``
program.
