AVbin

AVbin incorporates LGPL portions of Libav, http://libav.org

To install, run the included ``install.sh`` program.  Or, copy libavbin.*.dylib
to a path such as /usr/local/lib and create a symlink from it to libavbin.dylib
(without the version number). 
