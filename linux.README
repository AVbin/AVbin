AVbin

AVbin incorporates LGPL portions of Libav, http://libav.org

To install, run ``sudo ./install.sh``.  Or, copy libavbin.so.* to a path such
as /usr/lib and create a symlink from libavbin.so pointing to it.
